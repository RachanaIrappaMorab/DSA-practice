class Solution(object):
    def canJump(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        lastIndex=len(nums)-1
        for i in range(0,lastIndex):
            T=i+nums[i]
            if T>=lastIndex:
                return True
        return False
           
        
